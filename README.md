# PDF Studio Browser PWA

Browser-based PDF editor deployment repository.

The application processes opened PDFs, edits and OCR in the browser. The deployed application itself is public; users' opened PDF contents are not intentionally uploaded by the application code.

Deployment is performed with GitHub Pages / GitHub Actions. For confidential work, test first with a non-confidential sample PDF and verify browser behavior before production use.